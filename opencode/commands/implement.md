---
description: Use Ines to implement a bounded change and verify it proportionately.
---

Use `PROFILE: implement` for $ARGUMENTS. If no target is provided, implement the
agreed plan from the surrounding conversation. Delegate source changes to the
allowed worker. For a bug, require a Red-Green regression proof. Return the
changed files, executed checks, and residual risks. Do not commit or push unless
the user explicitly requested it.
